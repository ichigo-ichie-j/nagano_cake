<div class="container">
  <div class="row">
    <div class="col-md-7">
      <div class="my-2" style="max-width: 500px;">
        <img class="card-img" src="/assets/cake.jpeg" alt="">
        <p class="text-white">ようこそ、ながのCAKEへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でないかたも商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。</p>
      </div>

      <h3 class="md-3">新着商品</h3>
      <table class="table table-bordered">
        <tbody>
          <!--新着を4件表示-->
          <% @items.first(4). each do |item| %>
            <td>
              <%= link_to item_path(item.id) do %>
              <!--アイテム画像を表示-->
                <%= image_tag item.image, size: "300x200" %>
              <% end %>
              <!--商品名と税抜き価格のリンク表示-->
              <p><%= link_to item.name, item_path(item.id) %></p>
              <p><%= link_to item_path(item.id) do %>
                ¥<%= (item.non_taxed_price * 1.1).floor.to_s(:delimited) %> </p>
                  <% end %>
            </td>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <div>
    <div class="mb-4" align=right>
    <!--アイテム一覧画面に飛ぶ-->
   <%= link_to "全ての商品を見る", items_path %>
  </div>
</div>
