<div class="container">
	  <div class="row ml-3 mb-5 mt-3">
    <h2 class="head-line title_h2 text-left bg-light d-inline-block">マイページ</h2>
    </div>
   	<div class="row">
	      <h3>登録情報　<%= link_to "編集する", member_edit_path, class: 'btn btn-success' %></h4>
	      <table class="table table-bordered">
		       <thead>
						   <tr>
								 <th class="table-active">氏名</th>
								 <th><%= @member.last_name %> <%= @member.first_name %></th>
						   </tr>
							 <tr>
								 <th class="table-active">カナ</th>
								 <th><%= @member.last_name_kana %> <%= @member.first_name_kana %></th>
							 </tr>
							 <tr>
								 <th class="table-active">郵便番号</th>
								 <th><%= @member.zip_code %></th>
							 </tr>
							 <tr>
								 <th class="table-active">住所</th>
								 <th><%= @member.address %></th>
							 </tr>
							 <tr>
								 <th class="table-active">電話番号</th>
								 <th><%= @member.phone_number %></th>
							 </tr>
						   <tr>
								 <th class="table-active">メールアドレス</th>
								 <th><%= @member.email %></th>
							 </tr>
		   	   </thead>
		    </table>
	  <div>
		<P><strong>配送先　</strong>
		<%= link_to " 一覧を見る", shipping_addresses_path, class:"btn btn btn-primary" %>
		</p>
		<p></p>
		<p><strong>注文履歴</strong>
	 <%= link_to "一覧を見る", orders_path, class:"btn btn btn-primary" %>
   </p>
	 </div>
  </div>
</div>


